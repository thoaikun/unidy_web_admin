import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#FFC866'
        },
        secondary: {
            main: '#FFEDCB'
        }
    }
})

export default theme